import { Item, User, Shop } from './classes';

// Create a Shop instance
const shop = new Shop();
console.log('Create a new shop');

// Create a User instance
const user = new User('John Doe', 25);
console.log('Create a new user');

// Add items from the shop to the user's

